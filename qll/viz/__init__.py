"""Runnable physics explorers. Every plot calls the same qll functions the tests validate;
nothing is re-derived here. Run ``python -m qll.viz.<name>`` for an interactive window or
``python -m qll.viz.<name> --save docs/figures/<name>.svg`` for a headless figure."""
